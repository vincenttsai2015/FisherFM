_target_: src.data.bmnist_datamodule.BinaryMNISTDataModule
data_dir: ${paths.data_dir}
batch_size: 256 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 4
pin_memory: False